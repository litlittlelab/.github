# .github

Organization-wide defaults for the litlittlelab repos: the pull request template, issue templates (bug, feature, ops change, decision, incident) and the shared label list (`labels.json`, applied by each repo's "Sync labels" workflow).

This repo is public because GitHub only applies org-wide templates from a public `.github` repo. It never holds code, secrets or customer information.
